# 소미뷰티(SOMI BEAUTY) — 작업 기록

이 파일은 **이 프로젝트 폴더에 포함된 구현·에셋 내용**을 빠짐없이 정리한 참고용 문서입니다.  
새 채팅이나 다른 PC에서 이어서 작업할 때 이 파일과 `src/`, `index.html`, `public/`을 기준으로 하면 됩니다.

---

## 폴더 구조 요약

| 경로 | 역할 |
|------|------|
| `index.html`, `product.html`, `category.html` | 페이지 엔트리 |
| `src/style.css` | Tailwind + 전역 스타일·유틸(패딩, 벚꽃 등) |
| `src/main.js` | 벚꽃, 베스트셀러 탭 등 인터랙션 |
| `src/product-data.js` | 제품·카테고리 데이터 |
| `public/` | 빌드 시 `dist/` 루트로 복사되는 정적 파일(이미지 등) |
| `dist/` | `npm run build` 결과물(배포용) |

---

## PC 반응형·창 크기·브라우저 확대/축소 대응

**목표:** 창 리사이즈·줌에 맞춰 글자·이미지·여백이 자연스럽게 맞도록 조정.

**`src/style.css` (@layer base)**

- `html`: `font-size: clamp(14px, 0.28vw + 13.2px, 18px)` — `rem` 기준 글자 크기 유동화.
- `section`: `min-width: 0` — 그리드/플렉스 줄바꿈 시 깨짐 방지.
- `img`, `video`: `max-width: 100%`.

**`.padding-page-x`**

- 좌우 패딩을 `clamp` + 세이프 영역(`env(safe-area-inset-*)`)으로 유동 처리.

**`.hero-royal-frame`**

- 그림자 일부 `clamp` 적용.
- `max-height: min(88vh, 40rem)` — 히어로 영상 프레임이 과도하게 커지지 않도록 제한.

**`index.html`**

- `max-w-7xl` 컨테이너에 `w-full` 병행.
- 히어로 그리드: `gap` / `py`를 `clamp` 기반으로 유동화.
- 헤더: 로고·리본·데스크톱 네비 간격 유동화.
- 핑크 루틴, 시그니처, 베스트셀러, 카테고리 배너, 스킨케어 라인, 에디토리얼, 브랜드 스토리, 무드, 비주얼, 기프트, SNS, FAQ, 문의, 푸터 등: 섹션 세로 패딩·제목 크기·그리드 간격을 `clamp` 위주로 통일.
- 푸터: `py-[clamp(2.5rem,5vw,5rem)]`, 래퍼 `mx-auto w-full max-w-4xl`.

---

## SPECIAL CARE 배너 이미지 (토너 패드)

- **파일:** `public/images/cat-special.png`  
  (제공하신 PDRN 토너 패드 제품 사진)
- **참조:** `index.html` 카테고리 구역의 SPECIAL CARE 오른쪽 열 — `/images/cat-special.png`
- **표시:** `object-contain`, `w-full`, `h-auto`, `[max-height:min(32rem,65vh)]`, 연한 핑크 그라데이션·`rounded-xl` 프레임.

---

## 이전 대화에서 이미 반영된 사항(요약)

- 메타: OG/Twitter, canonical, 히어로·헤더 CTA, FAQ·문의 섹션 등.
- 베스트셀러: 탭·카드 정보·푸터 정리, `main.js` 접근성(키보드 탭, reduced-motion, 모바일 Escape 등).
- 카테고리/핑크 루틴/캠페인 등 **제품 상세 링크** 연결.
- **Makeup Serum** 제거: `product-data.js` 및 홈 베스트셀러·캠페인 등 연동 정리.
- 홈 **벚꽃 레이어** `#sakura-layer` + `main.js` + `style.css` 애니메이션, `prefers-reduced-motion` 시 비표시.
- 비주얼 캠페인 화살표·이미지 슬라이드 구성 조정(16장·4페이지 등).
- 모바일 세이프 영역·`padding-page-x` 등.

---

## 빌드·실행

```bash
npm install   # 최초 1회
npm run dev   # 개발 서버
npm run build # dist 갱신
```

배포 또는 USB로 옮길 때는 **`dist/` 전체**와 필요 시 `public/images`에 추가한 파일이 빌드에 포함되었는지 확인합니다.

---

## “기억”에 대해

AI 대화는 **세션이 끝나면 자동으로 이어지지 않습니다.**  
이후에도 같은 내용을 기준으로 하려면 **이 `프로젝트-작업기록.md`와 저장소 안의 파일**을 보면 됩니다.  
Cursor에서 프로젝트 규칙으로 쓰고 싶다면 `.cursor/rules`에 요약을 넣는 방법도 있습니다.

---

*마지막 업데이트: 작업 기록 통합 저장 시점.*
